ls /sys/firmware/efi/efivars
timedatectl set-ntp true
lsblk

cfdisk -z /dev/sda
{
EFI System for boot partation
Linux Swap for Swap partation
Linux root (x86-64) for root partation
Linux home for home partation
}

mkfs.fat -F 32 /dev/vda1
mkswap /dev/vda2
mkfs.ext4 /dev/vda3
mkfs.ext4 /dev/vda4

mount /dev/vda3 /mnt
mount --mkdir /dev/vda4 /mnt/home
mount --mkdir /dev/vda1 /mnt/boot/efi
swapon /dev/vda2

pacstrap -K /mnt base base-devel linux linux-firmware grub efibootmgr networkmanager vim git neofetch

genfstab -U /mnt >> /mnt/etc/fstab
arch-chroot /mnt

ln -sf /usr/share/zoneinfo/Asia/Kolkata /etc/localtime
hwclock --systohc
vim /etc/locale.gen
{
uncomment 'en_US UTF 8'
}

locale-gen

vim /etc/locale.conf
{
add LANG=en_US.UTF-8
}

vim /etc/hostname
{
add a host name preferebally archlinux or laptop or Aspire
}

passwd

useradd -m kalyan
passwd kalyan

vim /etc/sudoers
{
add user to sudoers list to execute commanda as sudo user
}

grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=GRUB
efibootmgr
grub-mkconfig -o /boot/grub/grub.cfg

systemctl enable --now NetworkManager

exit
reboot

vim /etc/pacman.conf
{
uncomment Color and ParallelDownloads and multilib
}

sudo vim /etc/makepkg.conf
{
uncomment and change value
MAKEFLAGS="-j6"
}

cd /opt
sudo git clone https://aur.archlinux.org/yay.git
sudo chown -R kalyan:kalyan yay
cd yay
makepkg -si

yay -S brave-bin
pacman -S xorg plasma plasma-wayland-session kde-applications

